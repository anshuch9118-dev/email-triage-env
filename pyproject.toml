[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "email-triage-env"
version = "0.1.0"
dependencies = [
    "requests",
    "fastapi",
    "uvicorn",
    "openai",
]

[project.scripts]
server = "app:main"
